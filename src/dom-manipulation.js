export function domCreator(ele) {
  return document.createElement(ele);
}

export function domSelector(ele) {
  return document.querySelector(ele);
}

export function domSelectorAll(ele) {
  return document.querySelectorAll(ele);
}
